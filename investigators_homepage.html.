<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Investigators Tools & Tactics</title>  <script src="https://cdn.tailwindcss.com"></script>  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            mono: ['"IBM Plex Mono"', 'monospace'],
            sans: ['system-ui', 'sans-serif'],
            display: ['"Orbitron"', 'system-ui', 'sans-serif'],
          },
          boxShadow: {
            glow: '0 0 0 1px rgba(34,211,238,0.15), 0 10px 35px rgba(2,8,23,0.65)',
          }
        }
      }
    }
  </script>  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@300;400;500;600&family=Orbitron:wght@600;700;800;900&display=swap" rel="stylesheet" />  <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>  <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>  <style>
    body { background-color: #020617; color: #cbd5e1; }
    ::-webkit-scrollbar { width: 6px; }
    ::-webkit-scrollbar-track { background: #020617; }
    ::-webkit-scrollbar-thumb { background: #1e293b; border-radius: 3px; }
  </style></head>
<body>
  <div id="root"></div>  <script type="text/babel">
    const NAV_ITEMS = [
      { label: 'HOME', href: 'index.html' },
      { label: 'TOOLS', href: 'tools.html' },
      { label: 'BLOG', href: 'blog.html' },
      { label: 'ABOUT', href: 'about.html' },
    ];

    const FEATURE_CARDS = [
      {
        title: 'Tools',
        href: 'tools.html',
        icon: '🧰',
        text: 'Custom investigator-built tools for OSINT, research, dork generation, workflow support, and digital casework.',
      },
      {
        title: 'Tactics & How-To',
        href: 'blog.html',
        icon: '📘',
        text: 'Practical field guides on online research, evidence capture, verification, search logic, and investigative tradecraft.',
      },
      {
        title: 'News & Analysis',
        href: 'blog.html',
        icon: '📰',
        text: 'Posts that break down current events, suspicious claims, public evidence, and investigative angles worth testing.',
      },
      {
        title: 'About the Investigator',
        href: 'about.html',
        icon: '🕵️',
        text: 'A full background page covering experience, training, specialties, and professional credibility in one place.',
      },
    ];

    const FEATURED_TOOLS = [
      {
        name: 'Google Dork Generator',
        status: 'Live',
        text: 'Build targeted search strings for people, corporate entities, and infrastructure research.',
      },
      {
        name: 'Subject Profile Builder',
        status: 'Planned',
        text: 'Organize identifiers, aliases, links, locations, and behavioral notes into one working profile.',
      },
      {
        name: 'Timeline Worksheet',
        status: 'Planned',
        text: 'Structure events, dates, contradictions, and source links into a clean investigative timeline.',
      },
    ];

    const LATEST_POSTS = [
      {
        category: 'HOW-TO',
        title: 'How to Search a Name Without Wasting Time',
        excerpt: 'A cleaner way to structure searches so you get signal instead of junk.',
      },
      {
        category: 'ANALYSIS',
        title: 'How to Read Public Claims Like an Investigator',
        excerpt: 'Separate raw claims, verified facts, inference, and noise before you form a conclusion.',
      },
      {
        category: 'FIELD NOTE',
        title: 'What Most People Miss in Basic OSINT Work',
        excerpt: 'The easy misses usually come from bad structure, weak source control, and premature certainty.',
      },
    ];

    function Navbar() {
      return (
        <nav className="border-b border-slate-800 bg-slate-950/80 backdrop-blur sticky top-0 z-50">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between">
            <a href="index.html" className="flex items-center gap-3">
              <div className="w-9 h-9 rounded-xl border border-cyan-500/30 bg-cyan-500/10 flex items-center justify-center text-cyan-400 font-bold shadow-glow">I</div>
              <div>
                <div className="font-mono text-[10px] text-cyan-400 tracking-[0.25em]">INVESTIGATORS</div>
                <div className="font-display text-sm sm:text-lg text-slate-100 tracking-[0.14em]">TOOLS &amp; TACTICS</div>
              </div>
            </a>

            <div className="hidden md:flex gap-6 font-mono text-xs font-semibold tracking-widest text-slate-400">
              {NAV_ITEMS.map((item) => (
                <a key={item.label} href={item.href} className="hover:text-slate-100 transition-colors">
                  {item.label}
                </a>
              ))}
            </div>
          </div>
        </nav>
      );
    }

    function Hero() {
      const heroImage = 'https://images.unsplash.com/photo-1765423946753-c79ae8b18031?auto=format&fit=crop&w=1600&q=80';

      return (
        <section className="relative overflow-hidden border-b border-slate-800">
          <div
            className="absolute inset-0 bg-cover bg-center"
            style={{ backgroundImage: `url(${heroImage})` }}
          />
          <div className="absolute inset-0 bg-slate-950/75" />
          <div className="absolute inset-0 bg-gradient-to-r from-slate-950 via-slate-950/80 to-slate-950/40" />

          <div className="relative max-w-7xl mx-auto px-4 sm:px-6 py-20 sm:py-28 lg:py-36">
            <div className="max-w-3xl">
              <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-cyan-500/20 bg-cyan-500/10 text-cyan-400 font-mono text-[10px] sm:text-xs tracking-[0.22em] uppercase mb-5">
                Digital Fieldcraft · Research · Analysis
              </div>

              <h1 className="font-display text-4xl sm:text-6xl lg:text-7xl leading-[0.95] text-white mb-6">
                Investigators <span className="text-cyan-400">Tools</span><br />
                &amp; Tactics
              </h1>

              <p className="text-slate-300 text-base sm:text-lg lg:text-xl max-w-2xl leading-relaxed mb-8">
                A working site for investigator-built tools, practical tradecraft, digital research guides, and hard-nosed analysis of the news and public evidence.
              </p>

              <div className="flex flex-col sm:flex-row gap-3">
                <a href="tools.html" className="px-6 py-3 rounded-xl bg-cyan-500/15 border border-cyan-400/30 text-cyan-300 font-mono text-sm tracking-widest font-bold text-center shadow-glow">
                  EXPLORE TOOLS
                </a>
                <a href="blog.html" className="px-6 py-3 rounded-xl bg-slate-900/70 border border-slate-700 text-slate-100 font-mono text-sm tracking-widest font-bold text-center">
                  READ THE BLOG
                </a>
              </div>
            </div>
          </div>
        </section>
      );
    }

    function CredibilityStrip() {
      const items = [
        '15+ years of investigative and law enforcement experience',
        'Interview and interrogation background',
        'Digital research, OSINT, and case-analysis focus',
        'Tools, tactics, and investigator-minded writing in one place',
      ];

      return (
        <section className="border-b border-slate-800 bg-slate-900/40">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 py-5 grid md:grid-cols-4 gap-4">
            {items.map((item, index) => (
              <div key={index} className="text-sm text-slate-300 border border-slate-800 rounded-xl bg-slate-950/60 px-4 py-4">
                {item}
              </div>
            ))}
          </div>
        </section>
      );
    }

    function MissionSection() {
      return (
        <section className="max-w-7xl mx-auto px-4 sm:px-6 py-16 sm:py-20">
          <div className="grid lg:grid-cols-[1.1fr_0.9fr] gap-8 items-start">
            <div>
              <div className="font-mono text-xs tracking-[0.25em] text-cyan-400 mb-3">SITE MISSION</div>
              <h2 className="text-3xl sm:text-4xl text-white font-bold mb-4">Built to be useful, not decorative.</h2>
              <p className="text-slate-400 text-base sm:text-lg leading-relaxed max-w-3xl">
                This site is meant to function like a digital field manual. The point is practical use: tools that solve problems, guides that teach real investigative process, and analysis that cuts through bad claims and lazy thinking.
              </p>
            </div>

            <div className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6 shadow-glow">
              <div className="font-mono text-xs tracking-[0.22em] text-slate-500 mb-4">WHAT BELONGS HERE</div>
              <div className="space-y-3 text-sm text-slate-300">
                <div>• Investigator-built tools and calculators</div>
                <div>• Tactical how-to articles and research workflows</div>
                <div>• News posts analyzed from an investigator lens</div>
                <div>• A real about page with background, training, and experience</div>
              </div>
            </div>
          </div>
        </section>
      );
    }

    function FeatureGrid() {
      return (
        <section className="max-w-7xl mx-auto px-4 sm:px-6 pb-16 sm:pb-20">
          <div className="mb-8">
            <div className="font-mono text-xs tracking-[0.25em] text-cyan-400 mb-3">MAIN SECTIONS</div>
            <h2 className="text-2xl sm:text-3xl text-white font-bold">What visitors can do here</h2>
          </div>

          <div className="grid md:grid-cols-2 xl:grid-cols-4 gap-5">
            {FEATURE_CARDS.map((card) => (
              <a key={card.title} href={card.href} className="group rounded-2xl border border-slate-800 bg-slate-900/50 p-6 hover:border-cyan-400/40 hover:bg-slate-900 transition-all shadow-glow">
                <div className="w-12 h-12 rounded-xl bg-slate-950 border border-slate-800 flex items-center justify-center text-2xl mb-5">
                  {card.icon}
                </div>
                <h3 className="text-white text-xl font-bold mb-3 group-hover:text-cyan-300 transition-colors">{card.title}</h3>
                <p className="text-sm text-slate-400 leading-relaxed">{card.text}</p>
                <div className="mt-5 font-mono text-xs tracking-[0.2em] text-cyan-400 uppercase">Open Section →</div>
              </a>
            ))}
          </div>
        </section>
      );
    }

    function FeaturedTools() {
      return (
        <section className="border-y border-slate-800 bg-slate-900/30">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 py-16 sm:py-20">
            <div className="mb-8">
              <div className="font-mono text-xs tracking-[0.25em] text-cyan-400 mb-3">FEATURED TOOLS</div>
              <h2 className="text-2xl sm:text-3xl text-white font-bold">A few things this platform can grow into</h2>
            </div>

            <div className="grid md:grid-cols-3 gap-5">
              {FEATURED_TOOLS.map((tool) => (
                <div key={tool.name} className="rounded-2xl border border-slate-800 bg-slate-950/70 p-6">
                  <div className="flex items-center justify-between gap-3 mb-4">
                    <h3 className="text-white font-bold text-lg">{tool.name}</h3>
                    <span className={`text-[10px] font-mono tracking-[0.2em] px-2 py-1 rounded-full border ${tool.status === 'Live' ? 'text-emerald-300 border-emerald-500/30 bg-emerald-500/10' : 'text-amber-300 border-amber-500/30 bg-amber-500/10'}`}>
                      {tool.status}
                    </span>
                  </div>
                  <p className="text-sm text-slate-400 leading-relaxed">{tool.text}</p>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    }

    function LatestPosts() {
      return (
        <section className="max-w-7xl mx-auto px-4 sm:px-6 py-16 sm:py-20">
          <div className="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-8">
            <div>
              <div className="font-mono text-xs tracking-[0.25em] text-cyan-400 mb-3">LATEST WRITING</div>
              <h2 className="text-2xl sm:text-3xl text-white font-bold">How-to, analysis, and field notes</h2>
            </div>
            <a href="blog.html" className="font-mono text-xs tracking-[0.2em] text-cyan-400 uppercase">View Full Archive →</a>
          </div>

          <div className="grid md:grid-cols-3 gap-5">
            {LATEST_POSTS.map((post) => (
              <article key={post.title} className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6 hover:border-slate-700 transition-colors">
                <div className="font-mono text-[10px] tracking-[0.22em] text-cyan-400 mb-3">{post.category}</div>
                <h3 className="text-white text-xl font-bold mb-3">{post.title}</h3>
                <p className="text-sm text-slate-400 leading-relaxed">{post.excerpt}</p>
              </article>
            ))}
          </div>
        </section>
      );
    }

    function AboutPreview() {
      return (
        <section className="border-t border-slate-800 bg-gradient-to-b from-slate-950 to-slate-900/60">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 py-16 sm:py-20">
            <div className="grid lg:grid-cols-[0.8fr_1.2fr] gap-8 items-center">
              <div className="rounded-3xl border border-slate-800 bg-slate-950/70 p-8 shadow-glow">
                <div className="w-16 h-16 rounded-2xl bg-cyan-500/10 border border-cyan-500/20 flex items-center justify-center text-3xl mb-5">🧾</div>
                <div className="font-mono text-xs tracking-[0.22em] text-slate-500 mb-3">ABOUT / CREDENTIALS</div>
                <h3 className="text-white text-2xl font-bold mb-3">A full background page that can also work like a resume.</h3>
                <p className="text-slate-400 text-sm leading-relaxed">
                  Use the about page to show training, assignments, specialties, experience, and the investigator mindset behind the site.
                </p>
              </div>

              <div>
                <h2 className="text-2xl sm:text-3xl text-white font-bold mb-4">This section should build trust without bloating the homepage.</h2>
                <p className="text-slate-400 text-base sm:text-lg leading-relaxed mb-6 max-w-3xl">
                  The homepage should prove there is real experience behind the material. The deep story belongs on the about page. Keep the front page tight, serious, and useful.
                </p>
                <a href="about.html" className="inline-flex px-6 py-3 rounded-xl bg-cyan-500/15 border border-cyan-400/30 text-cyan-300 font-mono text-sm tracking-widest font-bold shadow-glow">
                  VIEW FULL BACKGROUND
                </a>
              </div>
            </div>
          </div>
        </section>
      );
    }

    function Footer() {
      return (
        <footer className="border-t border-slate-800">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 py-8 flex flex-col sm:flex-row gap-3 sm:items-center sm:justify-between">
            <div className="font-mono text-[10px] sm:text-xs text-slate-600 tracking-[0.22em]">
              © {new Date().getFullYear()} INVESTIGATORS TOOLS &amp; TACTICS
            </div>
            <div className="font-mono text-[10px] sm:text-xs text-slate-500 tracking-[0.18em]">
              TOOLS · TRADECRAFT · ANALYSIS · EXPERIENCE
            </div>
          </div>
        </footer>
      );
    }

    function App() {
      return (
        <div className="min-h-screen bg-slate-950 text-slate-300 font-sans selection:bg-cyan-500/30">
          <Navbar />
          <Hero />
          <CredibilityStrip />
          <MissionSection />
          <FeatureGrid />
          <FeaturedTools />
          <LatestPosts />
          <AboutPreview />
          <Footer />
        </div>
      );
    }

    const root = ReactDOM.createRoot(document.getElementById('root'));
    root.render(<App />);
  </script></body>
</html>
